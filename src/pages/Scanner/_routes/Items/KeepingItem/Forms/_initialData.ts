import { IDetail } from "@src/types/detail";
import { IKeeping } from "@src/types/keeping";

const iniTIalData: IKeeping = {
  id: 0,
  detail: {} as IDetail,
  detailAmount: 1,
  keepingBox: undefined,
  keepingBoxAmount: 1,
};

export default iniTIalData;

name: Check push
on: [push]
jobs:
  Check-Code:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Install packages
        run: npm i
      - name: Lint
        run: npm run lint
      - name: Test
        run: npm run test

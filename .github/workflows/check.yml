name: Check push
on: [push]
jobs:
  Check-Code:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Lint
        run: npm run lint
      - name: Test
        run: npm run test
